server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/WaCoDiS/config-server.git
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"